let indice = 12, soma = 0, k = 1

do {
    k = k + 1
    soma = soma + k
    console.log(`${soma}`)
} while (k < indice)
    console.log(`Ao final do processamento, o valor da variável soma é: ${soma}`)
    
